# Contributing

## Workflow
1. Create a small focused change.
2. Run basic checks locally.
3. Open a pull request with context and expected outputs.

## Commit style
Use concise messages with optional scope, e.g. `feat(mash): add matrix helper`.

## Data policy
Do not commit large raw datasets, binary outputs, or HPC runtime artifacts.
